import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { SchoolStdPage } from './school-std';

@NgModule({
  declarations: [
    SchoolStdPage,
  ],
  imports: [
    IonicPageModule.forChild(SchoolStdPage),
  ],
})
export class SchoolStdPageModule {}
